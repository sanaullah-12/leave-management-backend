# PRODUCTION ENVIRONMENT VARIABLES
NODE_ENV=production 
PORT=5000

# PRODUCTION DATABASE - MongoDB Atlas
MONGODB_URI=mongodb+srv://qazisanaullah612_db_user:Uo9n6mtsevaGf1JY@cluster0.3ouqpxd.mongodb.net/leave-management?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration - Generate new secure secrets for production
JWT_SECRET=lms_prod_jwt_2024_secure_key_change_in_production_deployment_v1.0
JWT_EXPIRE=7d

# Frontend URL - Vercel deployment URL
FRONTEND_URL=https://leave-management-frontend-neon.vercel.app

# ---------------------------------------------------------------------------
# Email — Resend (HTTPS API, official SDK)
# ---------------------------------------------------------------------------
# RESEND_API_KEY is a SECRET — set it in the Railway dashboard only, never here.
#   Railway → service → Variables → RESEND_API_KEY = re_...
# Get a key at https://resend.com/api-keys
#
# EMAIL_FROM is THE single value to change when the verified domain is ready:
#   now:   Nexora <onboarding@resend.dev>   (Resend's shared test sender)
#   later: Nexora <no-reply@nexora.com>     (after verifying at resend.com/domains)
#
# NOTE: onboarding@resend.dev can ONLY deliver to the email address that owns
# the Resend account. Sending to anyone else returns success but never arrives.
EMAIL_FROM=Nexora <onboarding@resend.dev>

# CORS Configuration
ALLOWED_ORIGINS=https://leave-management-frontend-neon.vercel.app,https://leave-management-frontend-2j4tks98j.vercel.app
CORS_ORIGIN=https://leave-management-frontend-neon.vercel.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100